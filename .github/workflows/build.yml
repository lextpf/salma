name: build

# The Rust engine is the build. The C++ engine is retained only as the parity
# oracle and is gated by test.yml; it is no longer built here.
on:
  push:
    paths:
      - "rust/**"
      - "web/**"
      - "scripts/mo2-salma.py"
      - "build.bat"
      - "test.bat"
      - ".github/workflows/build.yml"
  pull_request:
    paths:
      - "rust/**"
      - "web/**"
      - "scripts/mo2-salma.py"
      - "build.bat"
      - "test.bat"
      - ".github/workflows/build.yml"

jobs:
  engine:
    name: engine (rust)
    runs-on: windows-latest
    defaults:
      run:
        working-directory: rust
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust

      - name: Format check
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --all-targets --release -- -D warnings

      - name: Build (release)
        run: cargo build --release

      - name: Test (release)
        run: cargo test --release

      # The three checks below are corpus-free, so they run on a clean runner.
      # The corpus-backed parity checks (compare_infer.py, run_harness.py) need
      # the mod archives and the C++ oracle DLL, neither of which exists here.
      - name: Package the deployable DLL
        run: python tools/package.py --no-build

      - name: Smoke test the C ABI
        run: python tools/smoke_ctypes.py target/release/mo2_salma_rs.dll

      - name: Smoke test the MO2 plugin loader
        run: python tools/smoke_plugin.py

      - name: Upload the deployable DLL
        uses: actions/upload-artifact@v4
        with:
          name: mo2-salma-dll
          path: rust/target/package/mo2-salma.dll
          if-no-files-found: error

  web:
    name: web dashboard
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: web/package-lock.json

      # `npm run build` is tsc (type-check) THEN vite build, so this is the
      # frontend's type gate as well as its build. Kept out of the engine job
      # because the SPA is served by the C++ mo2-server, which the Rust port
      # does not replace.
      - name: Build
        run: |
          npm ci
          npm run build
