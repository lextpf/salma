/**
 * @brief Label views that remain unavailable without backend data.
 * @author Alex (<https://github.com/lextpf>)
 *
 * These values are placeholders, not runtime feature switches.
 */
export const FEATURES = {
  conflicts: false,
  stateFlags: false,
} as const
